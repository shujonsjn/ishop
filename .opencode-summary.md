## Goal
- Modernize admin panel and product detail page in othoba.com style with tabs, specs table, Q&A, policy strip

## Constraints & Preferences
- Bengali/English i18n support for all UI text
- Admin credentials: admin@ishop.com / admin123
- Product detail rendered entirely client-side by `products.js loadProductDetail()` into `<div id="productDetail">`
- Inline `onclick` handlers require functions on `window` object (IIFE scope issue already fixed)
- ecommerce project at `C:\Users\atclm\OneDrive\Desktop\ecommerce`; Newspaper project at `Desktop/Newspaper/` — separate, Newspaper left untouched

## Progress
### Done
- **Othoba-style product detail page** — complete rewrite of `products.js loadProductDetail()`:
  - Gallery (left) + info card (right) layout with sticky gallery
  - Info card: featured/sale badges, title, star rating (clickable → reviews tab), price with compare price + discount %, brand pill, color swatches, qty selector, gradient buy now / add to cart buttons, share links (fb, whatsapp)
  - Delivery & Returns strip (🚚 shipping, 💵 COD, 🔄 return policy)
  - Tab navigation: Description | Specifications | Reviews (N) | Q&A
  - Specs table: Product Code (SKU/id), Brand, Category, Stock
  - Reviews tab: review cards + review form (rating select + textarea + submit)
  - Q&A tab: question textarea + submit + question list with answers
  - `window.switchPdTab()`, `window.submitQuestion()` for tab switching and Q&A submission
- **Product detail CSS** (`css/style.css`): clean othoba-style flat design — blue accent (#1a73e8), tabs, spec table, Q&A, policy strip, responsive mobile layout
- **i18n keys added**: 18 keys for tabs, specs, Q&A, policy, share, brand, vendor; plus 15 admin Q&A keys + `admin.sku`
- **Backend Q&A table + routes**: `product_questions` table, `GET /:id/questions` + `POST /:id/questions`
- **Admin Q&A management**: routes + page + admin.js functions + sidebar link on all admin pages
- **Root cause fix for tab not switching**: moved `window.switchPdTab` and `window.submitQuestion` before the detail page's early `return`
- **SKU field**: added `sku` column to products table, admin product form with `pfSku`, backend INSERT/UPDATE route support, admin.js form load/save

### In Progress
- (none)

### Blocked
- (none)

## Key Decisions
- Q&A uses same `product_questions` table for frontend and admin
- Admin Q&A uses `prompt()` for answer entry (can upgrade to modal later)
- SKU falls back to `p.id` if `p.sku` is empty
- Review count link uses `href="javascript:void(0)"` + `onclick="switchPdTab('reviews')"`
- Product detail uses clean flat design (#1a73e8 blue accent)
- `try { ALTER TABLE ... ADD COLUMN sku } catch {}` in db.js — runs on every startup, silently skips if column exists

## Next Steps
1. Restart server to apply `sku` column migration (or it'll apply on next restart)
2. Test product detail page at `http://localhost:3001/product.html?id=278` — tabs, reviews, Q&A
3. Test admin Q&A at `http://localhost:3001/admin/questions.html`
4. Test SKU field in admin product form at `http://localhost:3001/admin/product-new.html`

## Critical Context
- Tab switching broke initially because `switchPdTab` was inside IIFE but called from inline `onclick` — early `return` on detail page prevented `window.switchPdTab = ...` from executing
- Fix: `window.switchPdTab` and `window.submitQuestion` assigned BEFORE the detail-page early return (lines 85-111 in products.js)
- `loadQuestions(pid)` is a hoisted function declaration — accessible throughout IIFE scope
- SKU column: added via `ALTER TABLE` in db.js (try/catch), no migration file needed
- Admin sidebar: Q&A link added after categories, before users on all admin pages

## Relevant Files
- `js/products.js`: `loadProductDetail()` (~line 260), `switchPdTab`, `submitQuestion` (lines 85-111), `loadQuestions`
- `css/style.css`: product detail CSS — `/* ---- Othoba-style Product Detail ---- */` section
- `js/i18n.js`: all `detail.*` and `admin.*` translation keys
- `backend/routes/products.js`: Q&A routes `GET /:id/questions`, `POST /:id/questions`
- `backend/routes/admin.js`: `GET /questions`, `PUT /questions/:id/answer`; INSERT/UPDATE products with `sku`
- `backend/db.js`: `product_questions` table, `sku` column on products
- `admin/questions.html`: admin Q&A management page
- `admin/product-new.html`: admin product form with SKU field
- `js/admin.js`: `loadAdminQuestions()`, `showAnswerForm()`, product form load/save with SKU
- `admin/index.html`, `products.html`, `product-new.html`, `categories.html`, `users.html`, `orders.html`, `settings.html`, `profile.html`: all have Q&A sidebar link
